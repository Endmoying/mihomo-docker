services:
  clash:
    image: ghcr.io/Endmoying/mihomo-docker:main
    # build: .
    container_name: clash
    restart: always
    # command: [ "-d", "/root/.config/clash", "-ext-ui", "/dashboard", "-ext-ctl", "127.0.0.1:9090"] # please set ext-ctl in env not here
    volumes:
      - ./config:/root/.config/clash
    env_file: .env